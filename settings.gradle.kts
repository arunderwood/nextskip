pluginManagement {
    repositories {
        maven { url = uri("https://maven.vaadin.com/vaadin-prereleases") }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "nextskip"
