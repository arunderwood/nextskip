/**
 * Mock type for ConnectionState enum.
 */
enum ConnectionState {
  CONNECTED = 'CONNECTED',
  DISCONNECTED = 'DISCONNECTED',
  RECONNECTING = 'RECONNECTING',
  STALE = 'STALE',
}

export default ConnectionState;
